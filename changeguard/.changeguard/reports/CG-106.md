# ChangeGuard Brief — CG-106

## Enforce Route Consistency Between Bookings and Voyages

**Status:** ⚠️ Coordination required

### Heads up
You're tasked with implementing critical server-side validation and frontend synchronization to ensure booking routes always align with their assigned voyages. This ticket has significant overlaps with several other active tickets, requiring careful coordination, especially in `server/src/routes/bookings.ts`.

### Where you'll probably be working
This change will primarily involve updates across both the server and client:
*   **server/src/routes/bookings.ts** (Confidence: 0.98) — This is where you'll add the new validation logic to the `POST /api/bookings` and `PUT /api/bookings/:id` handlers to verify that the booking route matches the assigned voyage's route.
*   **client/src/pages/BookingFormPage.tsx** (Confidence: 0.95) — The frontend booking form will need updates to synchronize route and voyage selections, preventing incompatible combinations from being submitted.
*   **server/src/db/seed.ts** (Confidence: 0.90) — You'll need to update the seed data for booking BK-20250016 to align its route with voyage CAR-2503 as per requirements.

Tip: Treat these as starting points, not a fixed implementation plan. Verify the current code before making changes.

### Watch for collisions (Concurrent Work)
You have several significant overlaps with other tickets, primarily affecting shared API route handlers and the booking form:

*   **CG-101: Add Container Hold Status** (Confidence: 0.95)
    Both tickets modify `server/src/routes/bookings.ts`. CG-101 adds new business logic related to container hold status, while this ticket adds route validation. Coordinate to ensure your new validation integrates cleanly without disrupting CG-101's changes or vice-versa, especially around the POST/PUT handlers.

*   **CG-102: Block Billing Actions for Containers on Hold** (Confidence: 0.95)
    This ticket also modifies `server/src/routes/bookings.ts` (specifically the PUT endpoint for validation) and `client/src/pages/BookingFormPage.tsx`. CG-102 introduces billing action blocking for held containers. Synchronize frontend changes to `BookingFormPage.tsx` and backend validation logic to avoid conflicting UI states or unexpected validation cascades.

*   **CG-103: Emit Audit Events on Hold Status Transitions** (Confidence: 0.95)
    Like CG-101, this ticket modifies `server/src/routes/bookings.ts` by adding functional logic. While potentially less direct conflict than validation logic, ensure that new route validation doesn't inadvertently interfere with event emission logic being added by CG-103.

*   **CG-105: Refactor Booking Cutoff Logic into a Shared Policy Module** (Confidence: 0.95)
    This is a critical overlap. CG-105 is *refactoring* existing logic out of `server/src/routes/bookings.ts`, potentially affecting the same POST/PUT handlers where you'll be adding new validation. It is highly recommended to check the status of CG-105 first. If it's in progress or landed, you should base your work on its refactored structure to avoid merge conflicts and ensure your validation targets the correct, consolidated logic.

*   **CG-104: Add CSV Export for Bookings List** (Confidence: 0.75)
    While CG-104 primarily uses `server/src/routes/bookings.ts` as a reference for filter logic, your work involves heavy modifications to the POST/PUT handlers. Less direct conflict, but be aware of any shared helper functions or data access patterns that might be affected by CG-104's use.

### What this means for you (Action Plan)
Your primary concern is coordinating with other active work, particularly around `server/src/routes/bookings.ts` and `client/src/pages/BookingFormPage.tsx`. Start by checking the status of CG-105 and CG-102. If CG-105 is still open, consider coordinating with that developer to sequence your changes or work on a shared branch to integrate the refactor with your new validation. For the frontend, align with CG-102 to ensure a consistent user experience.

### Quick summary

| Aspect | Status / Guidance | Actionable Note |
|---|---|---|
| **Execution Readiness** | Coordination needed | Significant overlaps with multiple active tickets, primarily in `server/src/routes/bookings.ts` and `client/src/pages/BookingFormPage.tsx`. |
| **Prerequisites (Must Land First)** | None (No blocking upstream dependencies) | Begin coordination immediately. |
| **Concurrent Work & Overlaps** | CG-101, CG-102, CG-103, CG-105, CG-104 | Shared files: `server/src/routes/bookings.ts`, `client/src/pages/BookingFormPage.tsx`. |
| **Primary Merge Conflict Risk** | High risk with CG-105 (Refactor Booking Cutoff Logic) | Verify the status of CG-105 and base your work on its refactored structure if available, or coordinate on shared changes in `server/src/routes/bookings.ts`. |
| **Independent / Isolated Work** | None | All core tasks are impacted by concurrent work, requiring careful integration. |
| **Recommended First Step** | Coordinate with CG-105 and CG-102 | Before coding, verify the status of CG-105, especially regarding `server/src/routes/bookings.ts`, and discuss `client/src/pages/BookingFormPage.tsx` changes with the CG-102 developer. |