# ChangeGuard Brief — CG-102

## Block Billing Actions for Containers on Hold

**Status:** ⚠️ Prerequisites + coordination required

### Heads up
You are implementing billing restrictions for bookings in `HOLD` status. This involves both server-side request rejection and UI-level feedback. Expect heavy interaction with the booking lifecycle logic, which is currently undergoing refactoring.

### Before you start
*   **CG-101 (Add Container Hold Status):** This ticket introduces the base `HOLD` status. You cannot implement your logic until the status itself is part of the `LIFECYCLE` definition.
*   **CG-105 (Refactor Booking Cutoff Logic):** This ticket moves existing cutoff logic into a shared module. **Do not start CG-102 until this refactor is merged**, otherwise, you will face significant merge conflicts and potentially duplicate your logic in the legacy location.

### Suggested dependency order
CG-105 ↓ CG-101 ↓ CG-102

### Where you'll probably be working
*   `server/src/routes/bookings.ts`: Core logic for `PUT /api/bookings/:id`, the `checkCutoff` function, and your new `isOnHold` helper.
*   `client/src/pages/BookingFormPage.tsx`: UI feedback for locked billing fields.
*   `client/src/pages/BookingDetailPage.tsx`: Displaying the "Billing blocked" notice.

> Tip: Treat these as starting points, not a fixed implementation plan. Verify the current code before making changes.

### Watch for collisions
*   **CG-105:** You are both working on `server/src/routes/bookings.ts`. CG-105 is moving `checkCutoff` out; ensure you are adding your `isOnHold` helper to the *new* policy module created by CG-105, not the legacy file.
*   **CG-103:** Also touching `server/src/routes/bookings.ts` to add audit events for hold transitions. Keep an eye on PRs from the author of CG-103 to avoid stepping on each other's status-checking logic.
*   **CG-101:** You are both modifying the UI representation of hold statuses. Sync with the developer of CG-101 to ensure your "Billing blocked" notice integrates cleanly into the status banner they are building.

### What this means for you
First, confirm with the owners of CG-101 and CG-105 that their work is finalized. Once the `HOLD` status is available and the cutoff logic is moved, implement your `isOnHold` helper within the new shared policy module rather than the route handler. Coordinate with the CG-103 dev if you see them modifying the same status-transition blocks in `bookings.ts`.

### Quick summary
| | |
|---|---|
| Read first | CG-101, CG-105 |
| Coordinate with | CG-105 (Refactor), CG-103 (Auditing) |
| Highest collision risk | CG-105 (Shared logic migration) |
| Probably unrelated | CG-104 |
| Overall | ⚠️ Dependencies + coordination required |